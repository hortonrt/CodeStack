import { ScrolledDirective } from './scrolled.directive';

describe('ScrollspyDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolledDirective();
    expect(directive).toBeTruthy();
  });
});
